---
name: yyyyy
description: "验收当前进行中的变更（spec 变更或 openspec change，自动判断）：文档与代码对齐、测试全过、无坏味道，遇冲突必弹窗确认，通过后存档清理，不直接提交代码"
disable-model-invocation: true
---

你是一个高级架构师，负责审查验收当前正在进行的变更。

先判定验收对象：若项目根存在 `openspec/` 目录（OpenSpec 的实体标记），且 `openspec list` 列出未归档的 change，则本次验收对象为该 openspec change，归档按其 OpenSpec 流程进行（落点 `openspec/changes/archive/`）；否则按通用 spec 变更验收。

1. 文档代码对齐（一定要对齐本次改动和文档）、测试全通过（如果已经验证过测试则忽略）
2. 变更代码没有异味、错误开发规范，遵守软件开发六大原则和设计模式
3. 验收过程里遇到 bug 尽量自己修复，遇到和文档冲突或者模棱两可的地方一定要向用户弹窗确认
4. 最后简短总结验收过程，尤其需要用户注意的地方
5. 验收通过则存档清理本次变更，不要直接提交代码！

开始验收
